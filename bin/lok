#!/usr/bin/env node

var lok = require('../index');

if (process.argv[2] == '-h' || process.argv[2] == '--help') {
  console.log('\nusage: lok [directory] [port] \n');
  process.exit(0);
}

lok.server({
  folder: process.argv[2],
  port: process.argv[3]
});